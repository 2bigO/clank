#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

cd "$ROOT_DIR"

docker compose exec -T hermes-api \
  env PYTHONPATH=/opt/src/hermes-agent:/opt/src/tools:/workspace/host \
  python /workspace/host/tools/GatewaySmoke/telegram_ux_smoke.py "$@"
